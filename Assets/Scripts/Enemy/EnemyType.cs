/// <summary>
/// 적 종류. 인스펙터 드롭다운용.
/// 순서를 바꾸면 기존 Stage SO 선택이 밀리므로 새 타입은 끝에 추가한다.
/// </summary>
public enum EnemyType
{
    Enemy01 = 0,
    Enemy02,
    Enemy03,
    Enemy04,
    Enemy05,
    Enemy06
}
